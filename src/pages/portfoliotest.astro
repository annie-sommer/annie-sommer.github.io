<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Design by Annie Sommer</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,300;0,400;0,500;0,600;0,700;1,400&family=Karla:ital,wght@0,300;0,400;0,500;0,600;0,700;1,400&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <div class="hero-section">
            <h1 class="main-heading">Design by Annie Sommer</h1>
        </div>

        <div class="bio-section">
            <div class="bio-text">
                <p>Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry's standard dummy text ever since the 1500s, when an unknown printer took a galley of type and scrambled it to make a type specimen book. It has survived not only five centuries, but also the leap into electronic typesetting, remaining essentially unchanged. It was popularised in the 1960s with the release of Letraset sheets containing Lorem Ipsum passages, and more recently with desktop publishing software like Aldus PageMaker including versions of Lorem Ipsum.</p>
            </div>
            <div class="contact-info">
                <p>I live in Pasadena, California<br>
                anniesommer22 @ gmail</p>
            </div>
        </div>

        <div class="case-studies-section">
            <h2 class="section-title">Case Studies</h2>
            
            <div class="filter-tags" id="filterTags">
                <!-- Dynamic filter tags will be inserted here -->
            </div>

            <div class="case-studies-grid" id="caseStudiesGrid">
                <!-- Dynamic case study cards will be inserted here -->
            </div>
        </div>
    </div>

<style>
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Karla', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    background: #fafafa;
    color: #333;
    line-height: 1.6;
    font-size: 14pt;
}

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
}

.hero-section {
    margin-bottom: 4rem;
}

.main-heading {
    font-family: 'Montserrat', sans-serif;
    font-size: 3.5rem;
    font-weight: 400;
    color: #1a1a1a;
    margin-bottom: 1rem;
    line-height: 1.2;
    text-transform: uppercase;
    letter-spacing: 2px;
}

.bio-section {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 4rem;
    margin-bottom: 4rem;
}

.bio-text {
    font-size: 1rem;
    color: #666;
    line-height: 1.7;
}

.contact-info {
    text-align: right;
    font-size: 1rem;
    color: #666;
}

.case-studies-section {
    margin-top: 4rem;
}

.section-title {
    font-family: 'Montserrat', sans-serif;
    font-size: 2rem;
    font-weight: 400;
    color: #1a1a1a;
    margin-bottom: 2rem;
    text-transform: uppercase;
    letter-spacing: 1px;
}

.filter-tags {
    display: flex;
    gap: 1rem;
    margin-bottom: 3rem;
    flex-wrap: wrap;
    align-items: center;
}

.filter-tag {
    background: #fff;
    border: 1px solid #ddd;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    cursor: pointer;
    font-size: 0.9rem;
    font-weight: 500;
    transition: all 0.3s ease;
    user-select: none;
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.filter-tag:hover {
    background: #f0f0f0;
    transform: translateY(-1px);
}

.filter-tag.active {
    background: #333;
    color: white;
    border-color: #333;
}

.filter-tag .close-x {
    display: none;
    font-size: 1.1rem;
    font-weight: 600;
    line-height: 1;
}

.filter-tag.active .close-x {
    display: inline;
}

.show-all-btn {
    background: #666;
    color: white;
    border: 1px solid #666;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    cursor: pointer;
    font-size: 0.9rem;
    font-weight: 500;
    transition: all 0.3s ease;
    user-select: none;
}

.show-all-btn:hover {
    background: #555;
    transform: translateY(-1px);
}

.case-studies-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
    gap: 2rem;
    align-items: start;
}

.case-study-card {
    background: white;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.05);
    transition: opacity 0.3s ease, transform 0.3s ease;
    opacity: 1;
    transform: scale(1);
    width: 100%;
    min-height: 400px;
}

.case-study-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
}

.case-study-card.hidden {
    opacity: 0;
    transform: scale(0.95);
    pointer-events: none;
}

.case-study-image {
    width: 100%;
    height: 200px;
    background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.9rem;
    color: #999;
    flex-shrink: 0;
}

.case-study-content {
    padding: 2rem;
    display: flex;
    flex-direction: column;
    height: 200px;
}

.case-study-title {
    font-family: 'Montserrat', sans-serif;
    font-size: 1.3rem;
    font-weight: 600;
    color: #1a1a1a;
    margin-bottom: 1rem;
    line-height: 1.3;
}

.case-study-description {
    font-size: 1rem;
    color: #666;
    line-height: 1.6;
    margin-bottom: 1.5rem;
}

.case-study-tags {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
}

.case-study-tag {
    background: #f0f0f0;
    padding: 0.3rem 0.8rem;
    border-radius: 15px;
    font-size: 0.8rem;
    color: #666;
    font-weight: 500;
}

.filter-animation {
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

@media (max-width: 768px) {
    .container {
        padding: 1rem;
    }

    .main-heading {
        font-size: 2.5rem;
    }

    .bio-section {
        grid-template-columns: 1fr;
        gap: 2rem;
    }

    .contact-info {
        text-align: left;
    }

    .case-studies-grid {
        grid-template-columns: 1fr;
    }

    .filter-tags {
        justify-content: center;
    }
}

@media (max-width: 480px) {
    .main-heading {
        font-size: 2rem;
    }
}
</style>

<script>
(function() {
    'use strict';
    
    // Case studies data
    var caseStudies = [
        {
            name: "Tracking Plans and Data Catalog",
            description: "Building a large and game-changing feature from 0 to 1",
            tags: ["SAAS"]
        },
        {
            name: "Rudderstack UI",
            description: "Making a developer-first and backend software into a front-end beauty",
            tags: ["SAAS"]
        },
        {
            name: "Shark/Ninja",
            description: "Developing a voice design process for an emerging tech agency and a hit iOT product",
            tags: ["IoT", "Mobile", "Living Room"]
        },
        {
            name: "Prolitec",
            description: "Voice design for a first of it's kind iOT home scenting device",
            tags: ["IoT", "Mobile", "Living Room"]
        },
        {
            name: "Frontier Communications",
            description: "Mobile app UX/UI for a major new player in the ISP game",
            tags: ["IoT", "Mobile", "Living Room"]
        },
        {
            name: "Bread Financial",
            description: "Mobile app UX/UI for a fresh, personal, tech-first and friendly credit card",
            tags: ["Finance", "Mobile"]
        },
        {
            name: "Capital Group",
            description: "Internal ipad app for a 4T investment management firm",
            tags: ["Finance", "Mobile"]
        }
    ];

    // Get all unique tags
    var allTags = [];
    for (var i = 0; i < caseStudies.length; i++) {
        for (var j = 0; j < caseStudies[i].tags.length; j++) {
            if (allTags.indexOf(caseStudies[i].tags[j]) === -1) {
                allTags.push(caseStudies[i].tags[j]);
            }
        }
    }
    
    var activeTags = [];

    // Create filter tags
    function createFilterTags() {
        var filterContainer = document.getElementById('filterTags');
        
        // Add show all button first
        var showAllBtn = document.createElement('div');
        showAllBtn.className = 'show-all-btn';
        showAllBtn.textContent = 'Show All';
        showAllBtn.addEventListener('click', showAllCaseStudies);
        filterContainer.appendChild(showAllBtn);
        
        for (var i = 0; i < allTags.length; i++) {
            var tag = allTags[i];
            var tagElement = document.createElement('div');
            tagElement.className = 'filter-tag';
            tagElement.innerHTML = tag + '<span class="close-x">×</span>';
            tagElement.addEventListener('click', (function(tagName, element) {
                return function() {
                    toggleTag(tagName, element);
                };
            })(tag, tagElement));
            filterContainer.appendChild(tagElement);
        }
    }

    // Show all case studies (clear all filters)
    function showAllCaseStudies() {
        activeTags = [];
        var filterElements = document.querySelectorAll('.filter-tag');
        for (var i = 0; i < filterElements.length; i++) {
            filterElements[i].classList.remove('active');
        }
        filterCaseStudies();
    }

    // Toggle tag active state
    function toggleTag(tag, element) {
        var index = activeTags.indexOf(tag);
        if (index > -1) {
            activeTags.splice(index, 1);
            element.classList.remove('active');
        } else {
            activeTags.push(tag);
            element.classList.add('active');
        }
        filterCaseStudies();
    }

    // Create case study cards
    function createCaseStudyCards() {
        var gridContainer = document.getElementById('caseStudiesGrid');
        
        for (var i = 0; i < caseStudies.length; i++) {
            var study = caseStudies[i];
            var cardElement = document.createElement('div');
            cardElement.className = 'case-study-card filter-animation';
            cardElement.setAttribute('data-tags', study.tags.join(','));
            
            cardElement.innerHTML = 
                '<div class="case-study-image">' +
                    '[Project Image ' + (i + 1) + ']' +
                '</div>' +
                '<div class="case-study-content">' +
                    '<h3 class="case-study-title">' + study.name + '</h3>' +
                    '<p class="case-study-description">' + study.description + '</p>' +
                '</div>';
            
            gridContainer.appendChild(cardElement);
        }
    }

    // Filter case studies based on active tags
    function filterCaseStudies() {
        var cards = document.querySelectorAll('.case-study-card');
        
        for (var i = 0; i < cards.length; i++) {
            var card = cards[i];
            var cardTags = card.getAttribute('data-tags').split(',');
            
            if (activeTags.length === 0) {
                // Show all cards if no filters are active
                card.classList.remove('hidden');
            } else {
                // Show card if it has at least one active tag
                var hasActiveTag = false;
                for (var j = 0; j < activeTags.length; j++) {
                    if (cardTags.indexOf(activeTags[j]) > -1) {
                        hasActiveTag = true;
                        break;
                    }
                }
                
                if (hasActiveTag) {
                    card.classList.remove('hidden');
                } else {
                    card.classList.add('hidden');
                }
            }
        }
        
        // After animation completes, hide cards completely to remove from layout
        setTimeout(function() {
            for (var i = 0; i < cards.length; i++) {
                var card = cards[i];
                if (card.classList.contains('hidden')) {
                    card.style.display = 'none';
                } else {
                    card.style.display = 'block';
                }
            }
        }, 300);
    }

    // Initialize the page
    function init() {
        createFilterTags();
        createCaseStudyCards();
    }

    // Initialize when DOM is ready or immediately if already loaded
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init);
    } else {
        init();
    }
})();
</script>

</body>
</html>